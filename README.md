# Automated_GATK_var_call
Automated pipeline for variant call per field/group, to maximise variant retention.
This uses two Conda environments, which are listed in the YAML files.
Every step depends on the finishing of the previous one.
